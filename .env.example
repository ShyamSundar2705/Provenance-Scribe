# Used by docker-compose.yml to configure the Postgres container.
# Copy to .env (gitignored) and set a real password before running `docker compose up`.
# Must match the credentials in backend/.env's DATABASE_URL.
POSTGRES_USER=provenance
POSTGRES_PASSWORD=change-me
POSTGRES_DB=provenance_scribe
