# Must match the POSTGRES_USER/PASSWORD/DB in the root .env used by docker-compose.yml.
DATABASE_URL=postgresql+asyncpg://provenance:change-me@localhost:5432/provenance_scribe
JWT_SECRET=dev-secret-change-me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_HOURS=8
CORS_ORIGINS=["http://localhost:5173"]
# Verify the exact model string on the Groq console; names change.
GROQ_API_KEY=
GROQ_MODEL=
